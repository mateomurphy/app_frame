.block
  .secondary-navigation
    %ul.wat-cf
      %li.first= link_to "#{t("web-app-theme.list", :default => "List")}", collection_path
      %li.active= link_to "#{t("web-app-theme.new", :default => "New")}", new_resource_path
  .content
    %h2.title
      = t("web-app-theme.new", :default => "New")
      Post
    .inner
      = form_for :post, :url => collection_path, :html => { :class => :form } do |f|
        = render :partial => "form", :locals => {:f => f}
- content_for :sidebar, render(:partial => '/web_app_theme/actions/sidebar')
