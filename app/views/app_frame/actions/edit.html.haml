.page-header
  %h1= resource.to_s

= simple_form_for resource_path_array do |f|
  = render 'form', :f => f
  .actions
    = f.button :submit, :class => 'btn primary'
    = link_to "Cancel", collection_path, :class => 'btn'