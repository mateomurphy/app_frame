.page-header
  %h1 Editing #{resource_name}

= simple_form_for controller_namespaces + [resource] do |f|
  = render 'form', :f => f
  .actions
    = f.button :submit, :class => 'btn primary'
    = link_to "Cancel", collection_path, :class => 'btn'